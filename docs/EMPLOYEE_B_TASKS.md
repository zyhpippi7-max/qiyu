# 员工 B 专属任务说明

GitHub 用户名：`xilinasd123`

角色：私域运营交互、表单体验与本地质量验收负责人。

## 打开仓库后的第一步

Codex 必须先阅读根目录 `AGENTS.md`、本文件和 `docs/EMPLOYEE_DEVELOPMENT_RULES.md`。第一次只能检查页面和源码，不得修改。

先向负责人提交以下方案，等待负责人明确回复“同意修改”：

1. 当前交互的复现步骤和问题；
2. 新交互的完整操作顺序；
3. 准备修改的准确文件、组件和状态；
4. 对已有数据和另一名员工工作的影响；
5. 模拟数据、测试方法和验收标准。

## 负责内容

1. 完善微信好友选择：关键词搜索、标签筛选、已选列表、全选当前结果、清空和数量提示。
2. 保证 1200 名好友规模下可以快速定位，切换筛选后已选状态不丢失。
3. 优化群发和自动回复向导的字段名称、步骤顺序、返回修改、保存草稿和错误提示。
4. 补齐无设备、无好友、无聊天记录、任务失败、加载中等状态的交互反馈。
5. 检查关闭弹窗、取消任务、重复点击等操作不会误启动任务。
6. 只使用本地模拟数据补充渲染测试，整理复现步骤和验收记录。

## 默认允许文件

- `app/PrivateDomain.tsx`
- `app/ModulePages.tsx` 中负责人明确批准的私域片段
- `tests/rendered-html.test.mjs`

除非负责人单独书面批准，不得修改其他文件。

## 明确禁止

- 不改 API、数据库结构、登录权限、会员、支付和桌面自动化执行器。
- 不改 `app/api/**`、`desktop/**`、`db/**`、`drizzle/**`、`deploy/**`。
- 不改全局视觉体系、`package.json`、锁文件、构建配置和依赖。
- 不连接服务器，不读取生产聊天记录，不发送真实微信消息，不部署。
- 不执行 `git commit`、`git push`，不创建 PR。

## 本地验收

- 使用模拟的 1200 名联系人验证搜索、筛选和选择保持。
- 表单错误能定位到具体字段，关闭弹窗不会产生任务。
- 运行构建、渲染测试和 `git diff --check`。
- 将操作录像或截图、测试结果和完整 `git diff` 发给负责人；然后停止等待。

## 交给 Codex 的固定提示

> 你是员工 B 的本地辅助。先完整阅读 AGENTS.md、docs/EMPLOYEE_B_TASKS.md 和 docs/EMPLOYEE_DEVELOPMENT_RULES.md。现在只能只读分析并输出修改方案，负责人明确同意前不得编辑。获批后只能修改批准的私域交互和测试文件，必须使用模拟数据，完成本地验证后停止。禁止 commit、push、PR、部署、服务器连接、真实消息发送和范围扩张。
