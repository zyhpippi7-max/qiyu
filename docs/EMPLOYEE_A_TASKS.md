# 员工 A 专属任务说明

GitHub 用户名：`zyhpippi7-max`

角色：网站视觉、排版与响应式负责人。

## 打开仓库后的第一步

Codex 必须先阅读根目录 `AGENTS.md`、本文件和 `docs/EMPLOYEE_DEVELOPMENT_RULES.md`。第一次只能检查页面和源码，不得修改。

先向负责人提交以下方案，等待负责人明确回复“同意修改”：

1. 发现的视觉问题；
2. 希望调整后的效果；
3. 准备修改的准确文件和选择器；
4. 每项改动的风险；
5. 准备检查的屏幕尺寸和测试方法。

## 负责内容

1. 统一网站正文、标题、辅助文字、按钮、输入框和菜单的视觉规格。
2. 修复功能模块过小、文字难以阅读、页面留白不合理的问题。
3. 保证所有弹窗在页面中间显示，顶部和底部不被切除，内容较多时弹窗内部滚动。
4. 适配 1366×768、1440×900、1920×1080，并兼顾常见 Mac 笔记本屏幕。
5. 统一卡片间距、圆角、阴影、选中状态、错误状态、空状态和加载状态。
6. 提供修改前后对比截图，说明每项调整解决的问题。

## 默认允许文件

- `app/globals.css`
- 负责人明确点名的纯展示组件或文案文件

除非负责人单独书面批准，不得修改其他文件。

## 明确禁止

- 不改任何业务流程、接口请求、数据库和自动化逻辑。
- 不改 `app/api/**`、`desktop/**`、`db/**`、`drizzle/**`、`deploy/**`。
- 不改 `package.json`、锁文件、构建配置和依赖版本。
- 不连接服务器，不使用生产账号，不部署。
- 不执行 `git commit`、`git push`，不创建 PR。

## 本地验收

- 三种指定尺寸无裁切、遮挡、横向溢出和不可点击区域。
- 正文原则上不小于 14px，辅助文字原则上不小于 12px。
- 运行构建、渲染测试和 `git diff --check`。
- 将截图、测试结果和完整 `git diff` 发给负责人；然后停止等待。

## 交给 Codex 的固定提示

> 你是员工 A 的本地辅助。先完整阅读 AGENTS.md、docs/EMPLOYEE_A_TASKS.md 和 docs/EMPLOYEE_DEVELOPMENT_RULES.md。现在只能只读分析并输出修改方案，负责人明确同意前不得编辑。获批后只能修改批准的视觉文件，完成本地截图和测试后停止。禁止 commit、push、PR、部署、服务器连接和范围扩张。
