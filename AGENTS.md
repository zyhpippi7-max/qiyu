# 奇遇 AI 仓库协作边界

本文件对所有人工开发者及其使用的 Codex 生效。开始任务前必须先阅读本文、`docs/EMPLOYEE_DEVELOPMENT_RULES.md` 以及本人专属任务文件：

- GitHub 用户 `zyhpippi7-max`：阅读 `docs/EMPLOYEE_A_TASKS.md`
- GitHub 用户 `xilinasd123`：阅读 `docs/EMPLOYEE_B_TASKS.md`

## 当前阶段：仅限本地，必须先经负责人批准

1. 员工第一次打开仓库时只能阅读和检查，不得立即修改任何源码。
2. Codex 必须先向员工输出“问题理解、拟修改效果、拟修改文件、风险、验收方法”，由员工转交负责人查看。
3. 只有负责人明确回复“同意修改”后，才能在员工自己的电脑上进行对应修改。
4. 修改完成后只展示本地页面、截图和 `git diff` 给负责人检查。
5. 当前阶段禁止 `git commit`、`git push`、创建 Pull Request、合并和发布。负责人另行书面通知后才能进入提交阶段。
6. GitHub Issue 和远程员工分支只用于说明与预留，不代表已获得修改或提交许可。

## 工作原则

1. 只修改 Issue 明确列出的文件和验收项，不顺手重构、不扩展需求、不处理未分配问题。
2. 当前只允许在本地工作树修改，禁止任何远程提交行为。
3. 发现需要超出文件范围、修改接口字段或影响另一名员工时，立即停止并向负责人说明，等待批准。
4. 修改前后都要保存页面截图；交付负责人查看前至少运行与改动相关的测试。

## 严禁操作

- 禁止登录或修改生产服务器、宝塔、Nginx、PM2、域名、证书和下载文件。
- 禁止读取、复制、提交或展示密码、Cookie、Token、API Key、`.dev.vars`、`.env*`、数据库文件和用户数据。
- 禁止执行生产部署、数据库迁移、批量数据修改、真实微信消息发送或真实自动化任务。
- 未经负责人书面批准，禁止修改鉴权、会员权限、支付、数据库结构、自动化执行引擎和桌面端权限逻辑。
- 禁止升级依赖、替换框架、改变项目目录结构或大面积格式化文件。
- 禁止使用 `git push --force`、`git reset --hard`、删除分支、删除历史文件或绕过测试。
- 当前阶段禁止任何形式的 `git commit`、`git push`、Pull Request 和远程分支更新。

## Codex 使用限制

- 每次只给 Codex 一个 Issue；提示中必须写明“只完成此 Issue，不处理其他问题”。
- Codex 必须先输出将修改的文件清单；清单之外的文件不得编辑。
- 如果预计修改超过 5 个文件、300 行，或需要新增依赖，必须停止并申请拆分任务。
- Codex 不得自行部署、连接服务器、创建管理员、修改真实数据或调用真实发送接口。
- Codex 完成后只允许运行本地检查和测试，不允许提交、推送、创建 PR、合并或发布。

## 默认保护区域

除非 Issue 由负责人明确授权，员工和 Codex 不得修改：

- `app/api/**`
- `app/auth-server.ts`
- `db/**`、`drizzle/**`
- `desktop/**`
- `deploy/**`
- `.dev.vars`、`.env*`
- `package.json`、锁文件及构建配置

## 验收命令

视觉或交互修改至少运行：

```bash
pnpm run build
node --test tests/rendered-html.test.mjs
git diff --check
```

如测试失败，不得宣称“已完成”；应在本地验收说明中写清失败命令和原因，并交负责人查看。
